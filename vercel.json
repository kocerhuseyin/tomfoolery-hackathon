{
  "version": 2,
  "builds": [
    { "src": "frontend/package.json", "use": "@vercel/static-build" }
  ],
  "outputDirectory": "frontend/dist",
  "framework": "vite",
  "routes": [
    { "src": "/.*", "dest": "/index.html" }
  ]
}
